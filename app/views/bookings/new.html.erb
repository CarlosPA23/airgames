<%= 'Book this cool game' %>
<%= simple_form_for([@game, @booking]) do |f| %>
  <%= f.input :start_date %>
  <%= f.input :end_date %>
  <%= f.submit %>
<% end %>
<%= link_to "Back", game_path(@game) %>

<%# we may need to display the total price before booking it %>
